import * as React from 'react';
// import * as R from 'ramda';
// import styles from './other.less';
//
// interface ISection {
//   a: number;
// }

// export function Other(props: ISection) {
export function Other() {
  return (<div className="test">other</div>);
}

// const a = { 0: { a: 1 }, 1: { a: 1 } };
// const c = assignKey(R.values(R.map<Record<number, ISection>, Record<number, JSX.Element>>(Other, a)));
//
// export function assignKey(n: JSX.Element[]) {
//   return n.map((element, index) => React.cloneElement(element, { key: index }));
// }
